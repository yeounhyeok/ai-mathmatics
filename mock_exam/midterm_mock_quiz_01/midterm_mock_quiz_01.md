# 인공지능수학 중간고사 모의평가 01

시험 범위: 1-7주차  
형태: 손계산, 간단한 유도, 코드 빈칸 채우기  
총점: 30점  
제한 시간: 25분  

계산기 없이 푸는 기준입니다. 답만 적어도 되며, 유도 문제는 핵심 과정만 쓰면 됩니다.  
수식은 마크다운 호환성을 위해 일반 텍스트로 표기했습니다.

---

## 1. 벡터 내적 / 3점

```text
a = [2, -1, 3]
b = [4, 0, -2]

a dot b = ?
```

위 벡터의 내적을 구하시오.

---

## 2. 행렬 곱 / 4점

```text
A = [[ 1,  2],
     [ 3, -1]]

B = [[ 2,  0],
     [-1,  4]]

AB = ?
```

행렬곱 AB를 구하시오.

---

## 3. Shape / 3점

X의 shape이 `(5, 3)`, w의 shape이 `(3,)`일 때,

```python
X @ w
```

의 shape은?

---

## 4. MSE / 4점

```text
y     = [3, 1, 2]
y_hat = [2, 2, 4]

residual r = y - y_hat
MSE = (1/m) * sum((y_i - y_hat_i)^2)
```

잔차 벡터 r과 MSE를 구하시오.

---

## 5. 경사하강법 / 4점

현재 `w = 2`, 학습률 `alpha = 0.1`, `gradient = -3`일 때 업데이트 후 w는?

```text
w <- w - alpha * gradient
```

---

## 6. Sigmoid / 4점

```text
sigma(z) = 1 / (1 + e^(-z))
```

일 때, `sigma'(z)`를 `sigma(z)`로 표현하시오.  
그리고 `sigma(0)` 값은?

---

## 7. MLE / 5점

동전을 `n = 10`번 던져 앞면이 `k = 7`번 나왔다.  
이항분포에서 MLE로 추정한 `p_hat`은?

---

## 8. 코드 빈칸 / 3점

다음 빈칸을 채우시오.

```python
y_hat = X ___ w
grad = -(2/m) * X.___ @ (y - y_hat)
w = w - alpha * ___
```
